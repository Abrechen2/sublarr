services:
  sublarr:
    build: .
    container_name: sublarr
    ports:
      - "${SUBLARR_PORT:-5765}:5765"
    volumes:
      - ./config:/config
      - ${SUBLARR_MEDIA_PATH:-/media}:/media:rw
    env_file: .env
    restart: unless-stopped
